## YAML Template.
---
- hosts: localhost
  connection: local
  gather_facts: true
  
  tasks:
    - name: Check that you can connect (GET) to a page and it returns a status 200
      uri:
       url: http://www.example.com